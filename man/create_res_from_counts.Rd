% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/counts-to-ascat.R
\name{create_res_from_counts}
\alias{create_res_from_counts}
\title{Create ASCAT.sc result object from a raw counts file}
\usage{
create_res_from_counts(
  counts_file,
  output_dir,
  genome,
  build = "hg19",
  sex = "male",
  binsize = NULL,
  purs = seq(0.1, 1, 0.01),
  ploidies = seq(1.5, 5.5, 0.01),
  maxtumourpsi = 5,
  segmentation_alpha = 0.01,
  projectname = "counts",
  MC.CORES = 1
)
}
\arguments{
\item{counts_file}{Path to tab-separated counts file.  First column
\code{"bin"} with format \code{chr:start-end} (e.g. \code{1:1-500000}),
remaining columns are sample counts.}

\item{output_dir}{Directory for output profile files and the saved result
object.}

\item{genome}{A \code{BSgenome} object or the name of a BSgenome package
(e.g. \code{"BSgenome.Hsapiens.UCSC.hg19"}).}

\item{build}{Genome build identifier stored in the result object
(default \code{"hg19"}).}

\item{sex}{Sex of all samples: \code{"male"} or \code{"female"}
(default \code{"male"}).}

\item{binsize}{Bin size in bp.  If \code{NULL} (default), auto-detected from
the counts file.}

\item{purs}{Purity grid for ASCAT fitting
(default \code{seq(0.1, 1, 0.01)}).}

\item{ploidies}{Ploidy grid for ASCAT fitting
(default \code{seq(1.5, 5.5, 0.01)}).}

\item{maxtumourpsi}{Maximum tumour psi for fitting (default 5).}

\item{segmentation_alpha}{Alpha for CBS segmentation (default 0.01).}

\item{projectname}{Project name used when writing output files
(default \code{"counts"}).}

\item{MC.CORES}{Number of cores for parallel processing (default 1).}
}
\value{
An ASCAT.sc result object (list) with profiles, solutions, and
tracks.  Profile files are written to \code{output_dir}.
}
\description{
Takes a tab-separated file of pre-binned read counts (bins as rows, samples
as columns) and runs the full ASCAT.sc copy number calling pipeline.
This enables users who have already binned their data (e.g. at 500kb
resolution) to use ARMS without BAM files.
}
\examples{
\dontrun{
res <- create_res_from_counts(
  counts_file = "binned_counts.tsv",
  output_dir = "ascat_output",
  genome = "BSgenome.Hsapiens.UCSC.hg19",
  build = "hg19",
  sex = "male",
  projectname = "my_project"
)
# Load resulting profiles into ARMS
profiles <- load_ascat_profiles("ascat_output/")
}
}
