% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustering.R
\name{create_clusters_from_geojson_groups}
\alias{create_clusters_from_geojson_groups}
\title{Create cluster assignments from manually defined GeoJSON groups}
\usage{
create_clusters_from_geojson_groups(
  geojson_files,
  sample_names,
  unassigned_value = 0,
  verbose = TRUE
)
}
\arguments{
\item{geojson_files}{Character vector of paths to GeoJSON files (each file = one cluster)}

\item{sample_names}{Character vector of full sample names from your dataset}

\item{unassigned_value}{Cluster ID for samples not in any GeoJSON (default: 0 = excluded from pseudobulk)}

\item{verbose}{Logical, whether to print matching statistics (default: TRUE)}
}
\value{
Named integer vector of cluster assignments (names = sample names, values = cluster IDs)
}
\description{
This function allows you to manually define spatial groups using multiple GeoJSON files,
where each file represents a distinct cluster/group. The function assigns cluster IDs
based on which GeoJSON file contains each sample, creating cluster assignments compatible
with the pseudobulk workflow.
}
\examples{
\dontrun{
# Define manual groups via GeoJSON files
geojson_groups <- c(
  "tumor_region1.geojson",
  "tumor_region2.geojson",
  "normal_region.geojson"
)

# Create cluster assignments
manual_clusters <- create_clusters_from_geojson_groups(
  geojson_files = geojson_groups,
  sample_names = profiles$sample_names
)

# Use with pseudobulk workflow
pseudobulk <- create_pseudobulk_from_bins(
  "result.Rda",
  clusters = manual_clusters,
  sample_names = profiles$sample_names,
  output_dir = "pseudobulk_output"
)
}
}
